# Agent Instructions

## Before Starting Any Task

1. check `.agents/skills/` for a relevant skill and follow it if one matches. never skip this step.
2. read `readme.md` for project conventions, layout, and key concepts. never skip this step.
