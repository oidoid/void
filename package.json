{
  "private": true,
  "scripts": {
    "test:unit": "sh -c 'node --test ${@:-$(find src/ -name \\*.test.ts -printf %p\\ )}' --",
    "typecheck": "tsc --build"
  },
  "type": "module",
  "workspaces": [
    "src/**"
  ],
  "devDependencies": {
    "@oidoid/config": "0.0.12",
    "@types/node": "24.4.0",
    "@biomejs/biome": "2.4.10",
    "typescript": "6.0.2"
  }
}
