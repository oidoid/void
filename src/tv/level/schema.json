{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$defs": {
    "Ent": {
      "allOf": [
        {"$ref": "level-file.v0.json#/$defs/Ent"},
        {
          "type": "object",
          "properties": {
            "openFile": {
              "$ref": "#/$defs/OpenFile",
              "unevaluatedProperties": false
            },
            "tilePicker": {
              "$ref": "#/$defs/TilePicker",
              "unevaluatedProperties": false
            }
          }
        }
      ]
    },
    "OpenFile": {
      "type": "object",
      "description": "open file button config."
    },
    "TilePicker": {
      "type": "object",
      "description": "tile picker config.",
      "properties": {
        "tile": {
          "type": "integer",
          "description": "initial tile index.",
          "default": 0
        }
      }
    }
  }
}
