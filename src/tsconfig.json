// TypeScript config for all functional code.
{
  "extends": "../tools/tsconfig-base.json",
  "compilerOptions": {
    "lib": [
      "ES2024",
      "ESNext.disposable",
      "DOM",
      "DOM.AsyncIterable",
      "DOM.Iterable"
    ],

    "customConditions": ["browser"]
  },
  "exclude": ["${configDir}/**/*.test.*", "${configDir}/**/test/", "demo/"]
}
