// TypeScript config for demo.
{
  "extends": "@oidoid/config/tsconfig",
  "compilerOptions": {
    "lib": [
      "ES2024",
      "ESNext.disposable",
      "DOM",
      "DOM.AsyncIterable",
      "DOM.Iterable"
    ],

    "customConditions": ["browser"],

    "outDir": "../../dist/",
    "rootDir": "../../"
  },
  "references": [{"path": "../"}]
}
