// TypeScript config for demo.
{
  "extends": "@oidoid/config/tsconfig",
  "compilerOptions": {
    "lib": ["DOM", "ES2023", "ESNext.disposable"],

    "customConditions": ["browser"],

    "outDir": "../../dist/",
    "rootDir": "../../"
  },
  "references": [{"path": "../"}]
}
