// TypeScript config for demo.
{
  "extends": "../../tools/tsconfig-base.json",
  "compilerOptions": {
    "lib": [
      "ES2024",
      "ESNext.disposable",
      "DOM",
      "DOM.AsyncIterable",
      "DOM.Iterable"
    ],

    "customConditions": ["browser"]
  },
  "references": [{"path": "../"}]
}
