// TypeScript config for demo.
{
  "extends": "@oidoid/config/tsconfig",
  "compilerOptions": {
    "lib": [
      "DOM",
      "DOM.AsyncIterable",
      "DOM.Iterable",
      "ES2023",
      "ESNext.disposable"
    ],

    "customConditions": ["browser"],

    "outDir": "../../dist/",
    "rootDir": "../../"
  },
  "references": [{"path": "../"}]
}
